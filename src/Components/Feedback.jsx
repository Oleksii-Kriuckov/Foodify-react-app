import React from 'react'
import { Alert } from 'react-bootstrap'

const Feedback = () => {
    return (
        <Alert id='feedback' variant="success">
            Add to favourites
        </Alert>
    )
}

export default Feedback